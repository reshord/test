
const initialState = {
    contacts: {
        country: 'Ukraine', 
        city: 'Kiyv', 
        number: '+38023423498'
    },
}

const ContactsReducer = (state = initialState, action) => {
    return state
}

export default ContactsReducer;